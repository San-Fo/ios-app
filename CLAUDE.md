This directory contains the source code for an iOS app that allows people in Hong Kong to invest in local businesses.

The app is written in Swift and uses SwiftUI.

The project is generated using Tuist. Do not edit the Xcode configuration files directly if you can use the Tuist configuration instead.

If you're trying to run the `tuist` cli, do it using `mise x tuist@latest -- tuist <...>`.
